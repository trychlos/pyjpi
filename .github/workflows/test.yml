# testing reusable workflows

name: test_cov

on:
  workflow_dispatch:

env:
  VENV: venv

jobs:
  tests:
    uses: ./.github/workflows/_tests.yml
    secrets:
      codecov_token: "${{ secrets.CODECOV_TOKEN }}"
